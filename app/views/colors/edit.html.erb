<% if @color.errors.any? %>
  <% @color.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h1>Edit Color #<%= @color.id %></h1>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_color/<%= @color.id %>" class="form-horizontal">
      <!-- Label and input for color -->
      <div class="form-group">
        <label for="color" class="col-sm-2 control-label">
          Color
        </label>
        <div class="col-sm-10">
          <input type="text" id="color" name="color" placeholder="Color" class="form-control" value="<%= @color.color %>">
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <input type="submit" value="Update Color" class="btn btn-primary">
        </div>
      </div>
    </form>
  </div>
</div>

<div class="row">
  <div class="col-md-offset-3 col-md-6">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Edit Color #<%= @color.id %></h3>
      </div>
      <div class="panel-body">
        <form action="/update_color/<%= @color.id %>" class="form-horizontal">
          <!-- Label and input for color -->
          <div class="form-group">
            <label for="color" class="col-sm-2 control-label">
              Color
            </label>
            <div class="col-sm-10">
              <input type="text" id="color" name="color" placeholder="Color" class="form-control" value="<%= @color.color %>">
            </div>
          </div>

          <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
              <input type="submit" value="Update Color" class="btn btn-primary">
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
