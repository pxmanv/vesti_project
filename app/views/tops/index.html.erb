<div class="page-header">
  <h1>Tops</h1>
</div>

<p><a href="/tops/new" class="btn btn-primary">Add a New Top</a></p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Designer</th>
      <th>Season</th>
      <th>Fit</th>
      <th>Color</th>
      <th>Fabric</th>
      <th>Occasion</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tops.each do |top| %>
      <tr>
      <td><%= top.name.name %></td>
        <td><%= top.designer.designer %></td>
        <td><%= top.season.season %></td>
        <td><%= top.fit.fit %></td>
        <td><%= top.color.try(:color) %></td>
        <td><%= top.fabric.fabric %></td>
        <td><%= top.occasion.occasion %></td>
        <td>
          <a href="/tops/<%= top.id %>" class="btn btn-primary">
            <i class="fa fa-search-plus"></i>
          </a>
        </td>
        <td>
          <a href="/tops/<%= top.id %>/edit" class="btn btn-warning">
            <i class="fa fa-edit"></i>
          </a>
        </td>
        <td>
          <a href="/delete_top/<%= top.id %>" class="btn btn-danger">
            <i class="fa fa-trash-o"></i>
          </a>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

