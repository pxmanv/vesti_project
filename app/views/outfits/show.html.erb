<div class="page-header">
  <h1>Outfit #<%= @outfit.id %></h1>
</div>

<dl class="dl-horizontal">
  <dt>Top</dt>
  <dd><%= @top.designer.designer %> <%= @top.name.name %></dd>

  <dt>Bottom</dt>
  <dd><%= @bottom.designer.designer %> <%= @bottom.name.name %></dd>

  <dt>Outerwear</dt>
  <dd><%= @outerwear.designer.designer %> <%= @outerwear.name.name %></dd>

  <dt>Shoe</dt>
  <dd><%= @shoe.designer.designer %> <%= @shoe.name.name %></dd>

  <dt>Accessory</dt>
  <dd><%= @accessory.designer.designer %> <%= @accessory.name.name %></dd>

  <dt>User</dt>
  <dd><%= @user.try(:email) %></dd>

</dl>
