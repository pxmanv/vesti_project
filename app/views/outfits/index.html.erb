<div class="page-header">
  <h1>Outfits</h1>
</div>

<p><a href="/outfits/new" class="btn btn-primary">Add a New Outfit</a></p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Top</th>
      <th>Bottom</th>
      <th>Outerwear</th>
      <th>Shoe</th>
      <th>Accessory</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>


  <tbody>
    <% @outfits.each do |outfit| %>
    <tr>
      <td><img src="<%= outfit.top.image_url %>" class="image-over"></td>
      <td><img src="<%= outfit.bottom.image_url %>" class="image-over"></td>
      <td><img src="<%= outfit.outerwear.image_url %>" class="image-over"></td>
      <td><img src="<%= outfit.shoe.try(:image_url) %>" class="image-over"></td>
      <td><img src="<%= outfit.accessory.image_url %>" class="image-over"></td>

      <td>
        <a href="/outfits/<%= outfit.id %>" class="btn btn-primary">
          <i class="fa fa-search-plus"></i>
        </a>
      </td>
      <td>
        <a href="/outfits/<%= outfit.id %>/edit" class="btn btn-warning">
          <i class="fa fa-edit"></i>
        </a>
      </td>
      <td>
        <a href="/delete_outfit/<%= outfit.id %>" class="btn btn-danger">
          <i class="fa fa-trash-o"></i>
        </a>
      </td>
      <td>
            </div>
          </div>
        </form>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>

